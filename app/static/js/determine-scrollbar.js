const scrollbarVisible = (element) => {
  alert(hasHorizontalScrollbar);
  alert(hasVerticalScrollbar);
  
  if (x === true){
    return alert('scrollbar needed');
  } else {
    return alert('No Scroll Bar Neccessary');
}
}